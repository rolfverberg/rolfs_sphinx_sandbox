# Installation


## Using `pip`
rolfs_sphinx_sandbox is not yet available over PyPI. Once it is, run
```bash
pip install rolfs_sphinx_sandbox
```
to install.

## Using `conda`
rolfs_sphinx_sandbox is not yet available over conda-forge. Once it is, run
```bash
conda install rolfs_sphinx_sandbox
```
to install.
